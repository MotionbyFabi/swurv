
interface FooterProps {

}

const Footer: React.FC<FooterProps> = ({}) => {
        return (
          <>
            <footer />
          </>
        )
}
export default Footer;

// my prettier also doesn work
//you have to put "format on save" on vs code options, or just search in youtube, how yo use prettier :)
// I did do format on save but still doesnt owkr
//check youtube : prettier doesn't work, doesn't format :lmao